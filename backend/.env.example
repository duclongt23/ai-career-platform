MONGO_URI=mongodb://127.0.0.1:27017/ai-career-platform
JWT_SECRET=replace-with-a-random-secret
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_TTL_DAYS=30
FRONTEND_URL=http://localhost:5173
PASSWORD_RESET_TTL_MINUTES=30

# Optional SMTP configuration for password reset emails.
# When SMTP_HOST is empty, reset links are printed to the backend console.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=nglongdl11@gmai.com   
SMTP_PASS=qwlg bbfb hpcr wzrp
EMAIL_FROM=Career Guidance <your_email@gmail.com>"

ADMIN_SETUP_SECRET=replace-with-a-long-random-admin-setup-secret
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-pro

# Optional: enables current Vietnam job-market web search in Career Explore Chat.
TAVILY_API_KEY=
